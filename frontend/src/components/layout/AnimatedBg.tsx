/**
 * AnimatedBg — Futuristic animated circuit-board background.
 * Pure CSS animation, rendered once at the app root level.
 */
export default function AnimatedBg() {
  return <div className="circuit-bg" aria-hidden="true" />;
}
